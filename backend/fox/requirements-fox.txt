# Camoufox + deps for browserEngine=fox (used by backend/fox/camoufox_bridge.py).
# Install: pip install -r backend/fox/requirements-fox.txt
# Optional (geoip spoofing): pip install "camoufox[geoip]"
camoufox>=0.4.0
orjson>=3.9.0
