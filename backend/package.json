{
  "name": "account-control-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "proxy:diag": "node scripts/proxyPlaywrightDiagnostics.mjs",
    "playwright:install": "playwright install chromium"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "express": "^4.21.2",
    "playwright": "^1.59.1"
  }
}
